workflows:
  main-workflow:
    name: Ghost Ride Release Build
    instance_type: mac_mini_m2
    max_build_duration: 60
    environment:
      flutter: stable
      xcode: latest
      cocoapods: default
      groups:
        - google_play_signing # Deine Variablen für den Android Keystore
        - app_store_signing   # Deine Variablen für iOS (API Key, Issuer ID etc.)
    scripts:
      - name: Install dependencies
        script: |
          flutter pub get
      - name: Set up Android signing
        script: |
          mkdir -p android/app
          rm -f android/key.properties
          echo $CM_KEYSTORE | base64 --decode > android/app/upload-keystore.jks
          printf "keyAlias=%s\nkeyPassword=%s\nstorePassword=%s\nstoreFile=upload-keystore.jks\n" \
            "$CM_KEY_ALIAS" "$CM_KEY_PASSWORD" "$CM_KEYSTORE_PASSWORD" > android/key.properties
      - name: Flutter unit tests
        script: |
          [ -d "test" ] && flutter test || echo "Test directory not found, skipping tests..."
      - name: Set up iOS signing
        script: |
          keychain initialize
          # HIER GEÄNDERT: Wir sagen explizit --type app-store statt --type ios
          app-store-connect fetch-signing-files "$(xcode-project get-bundle-id)" --type app-store --install
          # Erzwingt die Profile für den Release-Build
          xcode-project use-profiles --type app-store
      - name: Build Android App Bundle
        script: |
          flutter build appbundle --release \
            --build-number=$BUILD_NUMBER
      - name: Build iOS IPA
        script: |
          # --no-codesign verhindert, dass Xcode mitten im Build nach lokalen Konten sucht
          flutter build ipa --release \
            --no-codesign \
            --build-number=$BUILD_NUMBER
    artifacts:
      - build/app/outputs/bundle/release/*.aab
      - build/ios/ipa/*.ipa
      - build/native_debug_symbols.zip
    publishing:
      email:
        recipients:
          - aw-garitz@web.de
        notify:
          success: true
          failure: true
      # Hier kannst du später Google Play und App Store Connect aktivieren
      # google_play:
      #   credentials: $GCLOUD_SERVICE_ACCOUNT_CREDENTIALS
      #   track: internal
      # app_store_connect:
      #   auth: integration